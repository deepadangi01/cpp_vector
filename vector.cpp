#include<iostream>
using namespace std;
#include<vector>
#include<iterator>
int main(){
    // vector<int>p{5,6,2,4,7,8};// dymenic vector
    // cout<<p.size();
    // vector<int>::iterator it=p.begin();
    // p.insert(it,30);
    // p.insert(it+4,{40,20,50});//form of list
    // for(auto k:p){
    //     cout<<k<<"\t";
    // }


    // vector<int>q(5,5);//static vector first parameter is --(size of vector) second parameter(vector's value) 
    // vector<string>str(1);
    

   // for(auto p=v.rbegin();p!=v.rend();p++)// using  for reverse vector
   pair<int,string>p;
   p.first=121;  // first and second is a kewword of pair
   p.second="cybrom";
   cout<<"roll no="<<p.first<<"\n";
   cout<<"name ="<<p.second;
   

    




}